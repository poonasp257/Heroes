#include "stdafx.h"

int _tmain() {
	//WSADATA wsa;
	//if (WSAStartup(MAKEWORD(2, 2), &wsa) != 0) {
	//	_tprintf(_T("WSAStartup() error!!\n"));
	//	return -1;
	//}

	//IOCPServer iocpServer;

	//while (iocpServer.Run()) {}

	//WSACleanup();
	return 0;
}